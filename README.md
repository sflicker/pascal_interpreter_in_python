# Pascal Interpreter in Python

This project is a Python implementation of a reduced Pascal interpreter.

## Project layout

- `src/pascal_interpreter/` contains the interpreter implementation.
- `test/` contains the automated tests and fixture files.
- `doc/` contains project documentation, including the Pascal grammar in `doc/pascal_grammar.bnf`.

For more details, see `doc/README.md`.
