This is an interpreter for a reduced version of Pascal written in Python.

## Project layout

- `src/pascal_interpreter/` contains the interpreter implementation and command-line entry module.
- `test/` contains the automated test cases plus the `.expr`, `.stat`, `.pas`, and `.exp` fixture files.
- `doc/` contains the project documentation and grammar reference, including `pascal_grammar.bnf`.
