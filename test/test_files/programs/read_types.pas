program read_types(input, output);

var
  i : Integer;
  r : Real;
  ch : Char;
  flag : Boolean;
  s : String;

begin
  read(i, r, ch, flag, s);
  writeln(i);
  writeln(r);
  writeln(ch);
  writeln(flag);
  writeln(s);
end.
