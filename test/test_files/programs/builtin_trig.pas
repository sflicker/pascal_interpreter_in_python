program builtin_trig;

var
  sinValue, cosValue, arctanValue : Real;

begin
  sinValue := sin(0);
  cosValue := cos(0);
  arctanValue := arctan(0);

  writeln(sinValue);
  writeln(cosValue);
  writeln(arctanValue);
end.
