program builtin_sqrt_exp_ln;

var
  rootValue, expValue, lnValue : Real;

begin
  rootValue := sqrt(9);
  expValue := exp(0);
  lnValue := ln(1);

  writeln(rootValue);
  writeln(expValue);
  writeln(lnValue);
end.
